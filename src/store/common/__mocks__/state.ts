import { CommonState, Mode } from '../types';

// eslint-disable-next-line import/prefer-default-export
export const state = {
    visibility: true as CommonState['visibility'],
    mode: Mode.NONE as CommonState['mode'],
};
